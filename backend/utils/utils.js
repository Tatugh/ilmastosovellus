//unused function for getting the last part of any given path. Possibly useful enough to NOT remove completely
// export const pathFinal = (reqPath) => {
//   let path = reqPath.split("/");
//   return path[path.length - 1];
// };
